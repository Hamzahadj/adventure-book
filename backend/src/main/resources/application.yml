spring:
  datasource:
    # TODO: Update this URL to match your local database environment (e.g., localhost, Docker container, or cloud instance)
    url: jdbc:postgresql://localhost:5432/adventure_db
    username: adventure
    password: adventure123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

server:
  port: 8080

game:
  initial-health: 10
logging:
  level:
    org.springframework.security: DEBUG