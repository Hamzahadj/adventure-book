services:
  postgres:
    image: postgres:latest
    container_name: adventure_postgres
    environment:
      POSTGRES_DB: adventure_db
      POSTGRES_USER: adventure
      POSTGRES_PASSWORD: adventure123
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql

volumes:
  pgdata: